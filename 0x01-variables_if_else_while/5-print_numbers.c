#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int disp_num = 0;

	while (disp_num < 10)

	{
		printf("%d", disp_num);
		disp_num++;
	}

	printf("\n");
	return (0);
}
