#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int disp_num;

	for (disp_num = 48; disp_num < 58; disp_num++)

	{
		putchar(disp_num);
	}
	putchar('\n');
	return (0);
}
